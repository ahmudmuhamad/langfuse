# 03 - Traces & Observations

Deep dive into the hierarchical observation model:
- Difference between **Spans**, **Generations**, and **Events**.
- Capturing inputs, outputs, timestamps, and model parameters.
- Enriching traces with custom **metadata**, **tags**, and log **levels** (DEBUG, DEFAULT, WARNING, ERROR).
