# 06 - Prompt Management

Managing and versioning prompts outside application code:
- Fetching managed prompts from the Langfuse UI (`langfuse.get_prompt()`).
- Compiling variables and linking generations to prompt versions.
- Promoting prompt versions (staging vs. production labels).
