# 04 - Sessions & Users

Organizing traces into conversations and user journeys:
- Linking traces to a conversation using `session_id`.
- Associating actions with specific users using `user_id`.
- Viewing session threads and user-level cost/token analytics in the Langfuse dashboard.
